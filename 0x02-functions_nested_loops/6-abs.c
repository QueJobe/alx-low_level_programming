#include "main.h"

/**
 * _abs - print absolute value of a number
 * @n will pass the number
 * Return: n the absoulte value
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
